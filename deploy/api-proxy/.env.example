# API Keys (copy from existing secrets)
ANTHROPIC_API_KEY=sk-ant-xxx
GEMINI_API_KEY=xxx

# Proxy Authentication Secret (64 character random string)
# Generate with: openssl rand -hex 32
PROXY_INTERNAL_SECRET=

# Server configuration (defaults)
PORT=3100
HOST=127.0.0.1
